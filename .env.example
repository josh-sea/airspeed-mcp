# Required: your Airspeed / Glyphic API key
GLYPHIC_API_KEY=your_key_here

# Optional: set this to protect the /sse endpoint with a bearer token
# If set, clients must send:  Authorization: Bearer <value>
MCP_AUTH_TOKEN=

# Port (Railway / Render set this automatically)
PORT=3000
